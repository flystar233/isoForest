% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_contribution.R
\name{feature_contribution}
\alias{feature_contribution}
\title{Calculate feature contributions to anomaly scores}
\usage{
feature_contribution(
  object,
  sample_ids = NULL,
  data,
  method = "path",
  contamination = 0.05,
  n_permutations = 15,
  max_trees = 50
)
}
\arguments{
\item{object}{An isoForest model object}

\item{sample_ids}{Vector of sample IDs to analyze (if NULL, analyzes all anomalies)}

\item{data}{The original training data}

\item{method}{Method for calculating contributions: "path" (default) or "permutation"}

\item{contamination}{Contamination rate for identifying anomalies (default 0.05)}

\item{n_permutations}{Number of permutations for permutation importance (default 15).
Lower values (10-15) are usually sufficient and much faster. Values above 20
show diminishing returns.}

\item{max_trees}{Maximum number of trees to analyze for path method (default 50)}
}
\value{
A list containing feature contributions for each analyzed sample
}
\description{
This function calculates how much each feature contributes to the anomaly score
of specific samples using path-based analysis or permutation importance.
}
\examples{
# Train model
model <- isoForest(iris[1:4])

# Analyze feature contributions for anomalous samples
contributions <- feature_contribution(model, data = iris[1:4])
print(contributions)

# Analyze specific samples with permutation method (fast)
contributions <- feature_contribution(model, sample_ids = c(1, 50), 
                                    data = iris[1:4], method = "permutation",
                                    n_permutations = 10)  # 10-15 recommended
}
